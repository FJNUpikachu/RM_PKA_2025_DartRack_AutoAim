Place your ONNX model file here.

Expected filename (default in `dart_bringup/config/node_params/detector_params.yaml`):
- `model/best.onnx`

If you want to use a different model, update `model_path` in `detector_params.yaml`.

